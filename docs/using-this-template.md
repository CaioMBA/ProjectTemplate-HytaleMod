# Using This Template

1. Rename the package `dev.ofatech.hytale.template` to your own namespace.
2. Update `gradle.properties` plugin values and `manifest.json` metadata.
3. Replace the example command and event registrations in `TemplatePlugin`.

Keep changes small and verify with `./gradlew build`.

